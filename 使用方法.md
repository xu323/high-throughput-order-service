# 使用方法（完全新手版）

> 這份文件假設你**完全沒碰過後端**，從零開始一步步把這個專案跑起來。
> 全程以 **Windows 11 + PowerShell** 為例。

## 目錄

1. [專案在做什麼](#1-專案在做什麼)
2. [系統整體架構](#2-系統整體架構)
3. [需要安裝什麼](#3-需要安裝什麼)
4. [從 PowerShell 啟動](#4-從-powershell-啟動)
5. [用 Docker Compose 啟動 MySQL / Redis / RabbitMQ / App](#5-用-docker-compose-啟動-mysql--redis--rabbitmq--app)
6. [單獨啟動 Spring Boot（不用 Docker 跑 app）](#6-單獨啟動-spring-boot不用-docker-跑-app)
7. [看 Swagger API 文件](#7-看-swagger-api-文件)
8. [建立商品](#8-建立商品)
9. [建立訂單](#9-建立訂單)
10. [測試扣庫存](#10-測試扣庫存)
11. [測試超賣防護](#11-測試超賣防護)
12. [查看 RabbitMQ 管理頁](#12-查看-rabbitmq-管理頁)
13. [查看 Redis cache](#13-查看-redis-cache)
14. [執行 Unit Test](#14-執行-unit-test)
15. [執行 Smoke Test](#15-執行-smoke-test)
16. [理解 ThreadPool / RabbitMQ / Redis Lock](#16-理解-threadpool--rabbitmq--redis-lock)
17. [推送到 GitHub](#17-推送到-github)
18. [常見錯誤排除](#18-常見錯誤排除)

---

## 1. 專案在做什麼

想像你是一個賣演唱會門票的網站工程師。一張限量門票放上去，**幾千個人同時搶**，
你會擔心什麼？

- 同時太多人下單，**伺服器掛掉**
- 庫存只有 50 張，但因為併發 bug **賣出 60 張**（超賣）
- 訂單建立後，使用者一直不付款，庫存被卡住
- 某個 email 通知服務掛了，整個下單流程跟著崩潰

這個專案就是用來**模擬**這些情境，並提供一套標準解法：

| 問題 | 解法 |
|---|---|
| 超賣 | MySQL 樂觀鎖 + Redis 分散式鎖（兩種策略可切換） |
| 通知 / email 不穩 | RabbitMQ 非同步處理，主流程不等通知 |
| 熱門商品讀爆 DB | Redis cache 快取商品資訊 |
| 客戶不付款卡住庫存 | 排程器每分鐘掃逾時訂單，自動取消並補回庫存 |
| 大量背景任務 | 自訂 ThreadPool 控制執行緒數量 |

---

## 2. 系統整體架構

```
你（瀏覽器 / Swagger UI）
        │ HTTP
        ▼
┌─────────────────────┐
│  Spring Boot App    │  ← Java 程式，跑在 8080
│  · Controller / API │
│  · Service          │
│  · JPA              │
└──────┬──────────────┘
       │ JDBC                        ┌─────────────┐
       ├──────────────────────────► │   MySQL     │  ← 訂單、商品、庫存
       │                             └─────────────┘
       │
       │                             ┌─────────────┐
       ├──────────────────────────► │   Redis     │  ← cache + lock
       │                             └─────────────┘
       │
       │  publish                    ┌─────────────┐
       ├─────────► order.exchange ► │  RabbitMQ   │
       │                             └─────┬───────┘
       │                                   │ subscribe
       │                                   ▼
       │                             各種 consumer：
       │                              · 寫稽核紀錄
       │                              · 模擬通知
       │                              · 失敗 N 次 → DLQ
       │
       │                             ┌─────────────┐
       └─────────────────────────►   │ ThreadPool  │  ← 背景任務
                                     └─────────────┘
```

---

## 3. 需要安裝什麼

| 工具 | 必須？ | 怎麼裝 | 備註 |
|---|---|---|---|
| **Docker Desktop** | 強烈建議 | https://www.docker.com/products/docker-desktop/ | 一鍵起 MySQL/Redis/RabbitMQ |
| **Git** | 是 | https://git-scm.com/download/win | 推送到 GitHub 用 |
| **JDK 21**（Temurin） | 想直接跑 Spring Boot 才需要 | https://adoptium.net/ | 不裝也能用 Docker 跑 |
| **Maven 3.9+** | 想直接編譯/測試才需要 | https://maven.apache.org/download.cgi | 不裝也能用 Docker 跑 |
| **PowerShell 7**（可選） | 否 | https://aka.ms/powershell | Win11 內建 PowerShell 5.1 也可 |

**驗證安裝**（在 PowerShell 跑）：

```powershell
docker version
git --version
java -version    # 想跑 Spring Boot 才需要
mvn -version     # 想跑 mvn 才需要
```

如果你**沒裝 Java/Maven**：完全沒問題，請只用「方式 A（Docker Compose）」啟動。

---

## 4. 從 PowerShell 啟動

把專案資料夾打開：

```powershell
cd C:\Users\Xu\JavaProjects\high-throughput-order-service
```

確認 `.env` 是否存在（沒有的話複製範本）：

```powershell
if (-not (Test-Path .env)) { Copy-Item .env.example .env }
```

接著選擇啟動方式：

- **方式 A**：完全用 Docker（推薦給新手）
- **方式 B**：MySQL/Redis/RabbitMQ 用 Docker，Spring Boot 在本機跑（適合開發）

---

## 5. 用 Docker Compose 啟動 MySQL / Redis / RabbitMQ / App

**方式 A：一鍵全部起（推薦給新手）**

```powershell
.\scripts\init-dev.ps1
```

這個腳本會做以下事：
1. 檢查 Docker 有開
2. 自動建立 `.env`
3. 跑 `docker compose up -d --build`
4. 等 4 個服務 healthy
5. 印出開啟用的網址

成功後你會看到：
```
應用程式：     http://localhost:8080
Swagger UI：   http://localhost:8080/swagger-ui/index.html
RabbitMQ UI：  http://localhost:15672  （帳號 guest / guest）
查看 logs：    docker compose logs -f app
```

**確認服務狀態**：

```powershell
docker compose ps
```

預期看到 4 個 container：`hto-app`、`hto-mysql`、`hto-redis`、`hto-rabbitmq`，狀態 `Up (healthy)`。

**看 app log**：

```powershell
docker compose logs -f app
```

按 `Ctrl + C` 離開 log 畫面（不會關掉 app）。

**停止全部**：

```powershell
docker compose down
```

**連資料一起清除**（會清掉 MySQL 資料）：

```powershell
docker compose down -v
```

---

## 6. 單獨啟動 Spring Boot（不用 Docker 跑 app）

**前提**：已安裝 JDK 21 + Maven。

```powershell
# 1) 只起 mysql / redis / rabbitmq，不起 app
docker compose up -d mysql redis rabbitmq

# 2) 等待健康（約 30 秒）
docker compose ps

# 3) 在本機跑 Spring Boot
mvn spring-boot:run
```

如果想要先打包成 jar 再跑：

```powershell
mvn clean package -DskipTests
java -jar target/high-throughput-order-service-0.1.0.jar
```

---

## 7. 看 Swagger API 文件

啟動後打開：

```
http://localhost:8080/swagger-ui/index.html
```

你會看到所有 API 的列表：
- **Health** — 健康檢查
- **Products** — 商品 CRUD + 庫存查詢
- **Orders** — 訂單建立、付款、取消、查事件
- **Demo / Admin** — seed、reset、併發測試

點任何一個 API → `Try it out` → 填參數 → `Execute`，就能直接呼叫，**不必額外裝 Postman**。

---

## 8. 建立商品

在 Swagger UI 裡找到 `POST /api/products`，按 `Try it out`，貼上：

```json
{
  "sku": "TEST-001",
  "name": "Test Product",
  "description": "first test product",
  "price": 199.00,
  "initialStock": 100
}
```

按 `Execute`，會回傳：
```json
{
  "success": true,
  "code": "OK",
  "data": { "id": 4, "sku": "TEST-001", "name": "Test Product", ... }
}
```

或在 PowerShell：

```powershell
$body = @{ sku='TEST-001'; name='Test Product'; description='x'; price=199; initialStock=100 } | ConvertTo-Json
Invoke-RestMethod -Method POST 'http://localhost:8080/api/products' -ContentType 'application/json' -Body $body
```

---

## 9. 建立訂單

```powershell
$body = @{
  userId = 1
  items = @(
    @{ productId = 1; quantity = 2 }
  )
  lockStrategy = 'OPTIMISTIC'
} | ConvertTo-Json
Invoke-RestMethod -Method POST 'http://localhost:8080/api/orders' -ContentType 'application/json' -Body $body
```

回傳的 `data.id` 就是這張訂單的 ID（假設是 `7`）。

接著付款：

```powershell
Invoke-RestMethod -Method POST 'http://localhost:8080/api/orders/7/pay'
```

或取消：

```powershell
Invoke-RestMethod -Method POST 'http://localhost:8080/api/orders/7/cancel'
```

查訂單發生過哪些事件：

```powershell
Invoke-RestMethod 'http://localhost:8080/api/orders/7/events'
```

---

## 10. 測試扣庫存

下單前先看一下庫存：

```powershell
Invoke-RestMethod 'http://localhost:8080/api/products/1/inventory'
```

下單一次後再查一次，`availableStock` 應該少 `quantity`。

如果取消了訂單，庫存會「**補回去**」。

---

## 11. 測試超賣防護

這是這個專案的核心。我們提供一個「壓測 API」：

```powershell
# 16 條執行緒同時下單，每條 10 筆，每筆扣 1 個
$body = @{
  productId        = 1
  threads          = 16
  ordersPerThread  = 10
  quantityPerOrder = 1
  lockStrategy     = 'OPTIMISTIC'
} | ConvertTo-Json
Invoke-RestMethod -Method POST 'http://localhost:8080/api/demo/concurrency-test' -ContentType 'application/json' -Body $body
```

如果商品 1 庫存原本是 100，結果會像：

```json
{
  "data": {
    "initialStock": 100,
    "remainingStock": 0,
    "actualSuccessOrders": 100,
    "failedOrders": 60,
    "elapsedMillis": 1234,
    "lockStrategy": "OPTIMISTIC",
    "overSold": false           ← 沒有超賣！
  }
}
```

把 `lockStrategy` 改成 `REDIS_LOCK` 再跑一次，比較兩種策略的差異（差異說明見 [docs/concurrency-design.md](docs/concurrency-design.md)）。

要重置資料：

```powershell
Invoke-RestMethod -Method POST 'http://localhost:8080/api/demo/reset'
Invoke-RestMethod -Method POST 'http://localhost:8080/api/demo/seed'
```

---

## 12. 查看 RabbitMQ 管理頁

打開 `http://localhost:15672`，用 `guest / guest` 登入。

你會看到：
- **Exchanges** 頁籤：找到 `order.exchange`，看它連到哪些 queue
- **Queues** 頁籤：可以看到每個 queue 的 message 數量
- 進入某個 queue → `Get messages` 可以「偷看」訊息內容

下單一筆，刷新看到 `order.created.queue` 多了一筆訊息（很快就會被 consumer 吃掉）。

---

## 13. 查看 Redis cache

最簡單的方式：

```powershell
docker compose exec redis redis-cli
```

進入 Redis CLI 後：

```
KEYS *               # 列所有 key
GET product:1        # 看商品 1 的 cache
GET inventory:1      # 看商品 1 的庫存 cache
TTL inventory:1      # 看剩多久過期
```

按 `exit` 離開。

---

## 14. 執行 Unit Test

```powershell
mvn test
```

你會看到一堆綠色的 ✓，例如：

```
[INFO] Tests run: 28, Failures: 0, Errors: 0, Skipped: 0
```

如果只想跑某一個測試：

```powershell
mvn test -Dtest=OrderServiceTest
```

整合測試（用 H2，不需要 Docker）：

```powershell
mvn verify
```

---

## 15. 執行 Smoke Test

確認 app 已啟動，然後：

```powershell
.\scripts\smoke-test.ps1
```

會自動跑：
1. `/health`
2. seed demo data
3. 列商品
4. 下單
5. 付款
6. 查事件

每一步都會印出結果。最後出現 `Smoke test passed.` 就代表通過。

---

## 16. 理解 ThreadPool / RabbitMQ / Redis Lock

### ThreadPool（執行緒池）

可以想成「一群隨時待命的工人」。

- **corePoolSize**：常駐工人，閒著也不會被裁掉
- **maxPoolSize**：尖峰時最多能再請來幾個臨時工
- **queueCapacity**：工人都在忙時，先把工作堆到等待區
- **拒絕策略**：等待區也滿了怎麼辦？我們用 `CallerRunsPolicy`，等同於「老闆自己跳下來做」，避免任務被丟掉

設定在 [src/main/resources/application.yml](src/main/resources/application.yml) 的 `async:` 區塊；環境變數可以覆蓋。

### RabbitMQ（訊息佇列）

可以想成「郵局」：
- **Exchange**：郵局收件處（`order.exchange`）
- **Routing Key**：信封上的地址（`order.created`、`order.paid`...）
- **Queue**：收件人的信箱
- **Consumer**：開信箱的人

訂單建立 → 寄信 `order.created` → 信進入 `order.created.queue` → consumer 拿出來處理。

如果 consumer 壞了，會 **retry 3 次**；都失敗的話訊息進入 **DLQ（死信佇列）**，可以人工處理或修 bug 後重放。

### Redis Lock（分散式鎖）

可以想成「廁所鑰匙」：
- 想進去 → `SET key uuid NX PX 5000`：成功 = 拿到鑰匙
- 沒搶到 → 等一下再試（最多 200ms）
- 用完 → 用 Lua 腳本「比對 uuid 後刪除」，避免拿到別人的鑰匙

詳細請見 [docs/concurrency-design.md](docs/concurrency-design.md)。

---

## 17. 推送到 GitHub

如果還沒初始化 git：

```powershell
git init
git branch -M main
git add .
git commit -m "Initial high throughput order service"
git remote add origin https://github.com/xu323/high-throughput-order-service.git
git push -u origin main
```

如果 remote 已存在：

```powershell
git remote set-url origin https://github.com/xu323/high-throughput-order-service.git
git push -u origin main
```

第一次 push 會跳出 GitHub 登入視窗（瀏覽器 OAuth）。如果 push 失敗：

| 錯誤 | 解法 |
|---|---|
| `Permission denied` | 確認 GitHub 帳號有這個 repo 的寫入權限 |
| `repository not found` | 確認 repo 已建立（可在瀏覽器先去 https://github.com/new 建空 repo） |
| 跳出 `gh auth login` | 跟著互動式提示走，選 HTTPS、用瀏覽器登入 |
| 卡在輸入密碼 | 改用 Personal Access Token（GitHub Settings → Developer settings → Tokens） |

---

## 18. 常見錯誤排除

| 症狀 | 原因 | 解法 |
|---|---|---|
| `docker compose up` 卡住或紅字 | Docker Desktop 沒開 | 開 Docker Desktop |
| port 3306 / 6379 / 5672 / 8080 已被佔用 | 本機原本就有 MySQL / Redis 等 | 改 `.env` 的 `*_PORT`，或停掉本機的服務 |
| `mvn` 找不到 | 沒裝 Maven | 用 Docker Compose；或從 https://maven.apache.org 安裝 |
| `java -version` 顯示 8 / 11 | 版本太舊 | 安裝 JDK 21（Temurin） |
| 啟動時 `Communications link failure` | MySQL 還沒 ready | 等 30 秒再試；或 `docker compose logs mysql` 看狀態 |
| Swagger UI 開不起來 | app 還沒啟動完 | 看 `docker compose logs -f app`，等到 `Started OrderServiceApplication` |
| 下單回 `INSUFFICIENT_STOCK` | 庫存真的不夠 | 呼叫 `POST /api/demo/reset` 然後 `POST /api/demo/seed` |
| 下單回 `LOCK_ACQUISITION_FAILED` | 樂觀鎖重試太多次或 Redis 鎖搶不到 | 降低併發、或檢查 Redis 是否異常 |
| `mvn test` 紅字 | JDK 不是 21 / 網路抓不到 dependency | 換 JDK 21；或先 `mvn -U dependency:resolve` |
| 推 GitHub 顯示 `403` | token 過期或權限不足 | 重新發 PAT，並 `git remote set-url` 換成 `https://<token>@github.com/...` |

更多請見 [docs/troubleshooting.md](docs/troubleshooting.md)。
