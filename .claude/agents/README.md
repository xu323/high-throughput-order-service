# .claude/agents

預留資料夾。本專案目前不使用 project-scoped subagents（一律由 Claude Code 主對話 + 本資料夾平行的 `../skills/` 提供領域指引）。

若未來要新增 subagent，可在這裡放 `*.md`，遵循 [Claude Code subagent 文件](https://docs.claude.com/en/docs/claude-code/sub-agents)。
